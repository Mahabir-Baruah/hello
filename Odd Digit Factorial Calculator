
n = input("Enter number: ")

for ch in n:
    digit = int(ch)

    if digit % 2 != 0:
        fact = 1

        for i in range(1, digit + 1):
            fact *= i

        print(digit, "! =", fact)
